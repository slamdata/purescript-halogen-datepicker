{
  "private": true,
  "scripts": {
    "watch": "pulp --watch build --to dist/example.js",
    "build": "pulp build --to dist/example.js"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.0",
    "purescript-psa": "^0.5.0"
  }
}
